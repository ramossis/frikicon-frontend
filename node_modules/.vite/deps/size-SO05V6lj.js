import { I as includes, u as getCurrentInstanceName, ut as propsFactory, v as convertToUnit, x as destructComputed } from "./defineComponent-BW8G7rbF.js";
//#region node_modules/vuetify/lib/composables/size.js
var predefinedSizes = [
	"x-small",
	"small",
	"default",
	"large",
	"x-large"
];
var makeSizeProps = propsFactory({ size: {
	type: [String, Number],
	default: "default"
} }, "size");
function useSize(props, name = getCurrentInstanceName()) {
	return destructComputed(() => {
		const size = props.size;
		let sizeClasses;
		let sizeStyles;
		if (includes(predefinedSizes, size)) sizeClasses = `${name}--size-${size}`;
		else if (size) sizeStyles = {
			width: convertToUnit(size),
			height: convertToUnit(size)
		};
		return {
			sizeClasses,
			sizeStyles
		};
	});
}
//#endregion
export { useSize as n, makeSizeProps as t };
