import { W as computed } from "./vue.runtime.esm-bundler-CZzmpwlB.js";
import { ut as propsFactory, v as convertToUnit } from "./defineComponent-BW8G7rbF.js";
//#region node_modules/vuetify/lib/composables/dimensions.js
var makeDimensionProps = propsFactory({
	height: [Number, String],
	maxHeight: [Number, String],
	maxWidth: [Number, String],
	minHeight: [Number, String],
	minWidth: [Number, String],
	width: [Number, String]
}, "dimension");
function useDimension(props) {
	return { dimensionStyles: computed(() => {
		const styles = {};
		const height = convertToUnit(props.height);
		const maxHeight = convertToUnit(props.maxHeight);
		const maxWidth = convertToUnit(props.maxWidth);
		const minHeight = convertToUnit(props.minHeight);
		const minWidth = convertToUnit(props.minWidth);
		const width = convertToUnit(props.width);
		if (height) styles.height = height;
		if (maxHeight) styles.maxHeight = maxHeight;
		if (maxWidth) styles.maxWidth = maxWidth;
		if (minHeight) styles.minHeight = minHeight;
		if (minWidth) styles.minWidth = minWidth;
		if (width) styles.width = width;
		return styles;
	}) };
}
//#endregion
export { useDimension as n, makeDimensionProps as t };
