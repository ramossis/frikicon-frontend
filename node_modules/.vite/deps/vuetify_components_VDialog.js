import { Ot as mergeProps, _n as watch, kt as nextTick, qn as ref, tt as createVNode } from "./vue.runtime.esm-bundler-CZzmpwlB.js";
import { U as noop, W as omit, k as getActiveElement, n as genericComponent, ut as propsFactory } from "./defineComponent-BW8G7rbF.js";
import { t as useRender } from "./useRender-BeL2a1eu.js";
import { _ as VDialogTransition } from "./transitions-StSMh2Xu.js";
import { n as makeVOverlayProps, t as VOverlay } from "./VOverlay-c36q3Lmx.js";
import { t as VDefaultsProvider } from "./VDefaultsProvider-DooUtWB6.js";
import { t as useProxiedModel } from "./proxiedModel-9GbPH9gT.js";
import { t as forwardRefs } from "./forwardRefs-B6GOH1g6.js";
import { t as useScopeId } from "./scopeId-BLjeJkoo.js";
import "/home/ramos/Escritorio/proyecto_frikivon/friki_con/node_modules/vuetify/lib/components/VDialog/VDialog.css";
//#region node_modules/vuetify/lib/components/VDialog/VDialog.js
var makeVDialogProps = propsFactory({
	fullscreen: Boolean,
	scrollable: Boolean,
	...omit(makeVOverlayProps({
		captureFocus: true,
		location: "center center",
		origin: "center center",
		scrollStrategy: "block",
		transition: { component: VDialogTransition },
		zIndex: 2400,
		retainFocus: true
	}), ["disableInitialFocus"])
}, "VDialog");
var VDialog = genericComponent()({
	name: "VDialog",
	props: makeVDialogProps(),
	emits: {
		"update:modelValue": (value) => true,
		afterEnter: () => true,
		afterLeave: () => true
	},
	setup(props, { emit, slots }) {
		const isActive = useProxiedModel(props, "modelValue");
		const { scopeId } = useScopeId();
		const overlay = ref();
		function onAfterEnter() {
			emit("afterEnter");
			if ((props.scrim || props.retainFocus) && overlay.value?.contentEl && !overlay.value.contentEl.contains(getActiveElement())) overlay.value.contentEl.focus({ preventScroll: true });
		}
		function onAfterLeave() {
			emit("afterLeave");
		}
		watch(isActive, async (val) => {
			if (!val) {
				await nextTick();
				overlay.value?.activatorEl?.focus({ preventScroll: true });
			}
		});
		useRender(() => {
			const overlayProps = VOverlay.filterProps(props);
			const activatorProps = mergeProps({ "aria-haspopup": "dialog" }, props.activatorProps);
			const contentProps = mergeProps({ tabindex: -1 }, props.contentProps);
			const locationStrategy = props.fullscreen ? noop : props.locationStrategy;
			return createVNode(VOverlay, mergeProps({
				"ref": overlay,
				"class": [
					"v-dialog",
					{
						"v-dialog--fullscreen": props.fullscreen,
						"v-dialog--scrollable": props.scrollable
					},
					props.class
				],
				"style": props.style
			}, overlayProps, {
				"modelValue": isActive.value,
				"onUpdate:modelValue": ($event) => isActive.value = $event,
				"aria-modal": "true",
				"activatorProps": activatorProps,
				"contentProps": contentProps,
				"height": !props.fullscreen ? props.height : void 0,
				"width": !props.fullscreen ? props.width : void 0,
				"maxHeight": !props.fullscreen ? props.maxHeight : void 0,
				"maxWidth": !props.fullscreen ? props.maxWidth : void 0,
				"locationStrategy": locationStrategy,
				"role": "dialog",
				"onAfterEnter": onAfterEnter,
				"onAfterLeave": onAfterLeave
			}, scopeId), {
				activator: slots.activator,
				default: (...args) => createVNode(VDefaultsProvider, { "root": "VDialog" }, { default: () => [slots.default?.(...args)] })
			});
		});
		return forwardRefs({}, overlay);
	}
});
//#endregion
export { VDialog };
