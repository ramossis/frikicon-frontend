import { $n as toRef, G as createBaseVNode, W as computed, cr as normalizeStyle, or as normalizeClass } from "./vue.runtime.esm-bundler-CZzmpwlB.js";
import { n as genericComponent, ut as propsFactory, v as convertToUnit } from "./defineComponent-BW8G7rbF.js";
import { n as makeComponentProps, t as useRender } from "./useRender-BeL2a1eu.js";
import { r as useTextColor } from "./color-BOsIBON5.js";
import { i as provideTheme, r as makeThemeProps } from "./theme-D5Vm6Ccl.js";
import "/home/ramos/Escritorio/proyecto_frikivon/friki_con/node_modules/vuetify/lib/components/VDivider/VDivider.css";
//#region node_modules/vuetify/lib/components/VDivider/VDivider.js
var allowedVariants = [
	"dotted",
	"dashed",
	"solid",
	"double"
];
var makeVDividerProps = propsFactory({
	color: String,
	contentOffset: [
		Number,
		String,
		Array
	],
	gradient: Boolean,
	inset: Boolean,
	length: [Number, String],
	opacity: [Number, String],
	thickness: [Number, String],
	vertical: Boolean,
	variant: {
		type: String,
		default: "solid",
		validator: (v) => allowedVariants.includes(v)
	},
	...makeComponentProps(),
	...makeThemeProps()
}, "VDivider");
var VDivider = genericComponent()({
	name: "VDivider",
	props: makeVDividerProps(),
	setup(props, { attrs, slots }) {
		const { themeClasses } = provideTheme(props);
		const { textColorClasses, textColorStyles } = useTextColor(() => props.color);
		const dividerStyles = computed(() => {
			const styles = {};
			if (props.length) styles[props.vertical ? "height" : "width"] = convertToUnit(props.length);
			if (props.thickness) styles[props.vertical ? "borderRightWidth" : "borderTopWidth"] = convertToUnit(props.thickness);
			return styles;
		});
		const contentStyles = toRef(() => {
			const margin = Array.isArray(props.contentOffset) ? props.contentOffset[0] : props.contentOffset;
			const shift = Array.isArray(props.contentOffset) ? props.contentOffset[1] : 0;
			return {
				marginBlock: props.vertical && margin ? convertToUnit(margin) : void 0,
				marginInline: !props.vertical && margin ? convertToUnit(margin) : void 0,
				transform: shift ? `translate${props.vertical ? "X" : "Y"}(${convertToUnit(shift)})` : void 0
			};
		});
		useRender(() => {
			const role = attrs.role;
			const hasSlot = !!slots.default;
			const divider = createBaseVNode("hr", {
				"class": normalizeClass([
					{
						"v-divider": true,
						"v-divider--gradient": props.gradient && !slots.default,
						"v-divider--inset": props.inset,
						"v-divider--vertical": props.vertical
					},
					themeClasses.value,
					textColorClasses.value,
					props.class
				]),
				"style": normalizeStyle([
					dividerStyles.value,
					textColorStyles.value,
					{ "--v-border-opacity": props.opacity },
					{ "border-style": props.variant },
					props.style
				]),
				"aria-orientation": !hasSlot && !role ? props.vertical ? "vertical" : "horizontal" : void 0,
				"role": !hasSlot ? role : void 0
			}, null);
			if (!hasSlot) return divider;
			return createBaseVNode("div", { "class": normalizeClass(["v-divider__wrapper", {
				"v-divider__wrapper--gradient": props.gradient,
				"v-divider__wrapper--inset": props.inset,
				"v-divider__wrapper--vertical": props.vertical
			}]) }, [
				divider,
				createBaseVNode("div", {
					"class": "v-divider__content",
					"style": normalizeStyle(contentStyles.value)
				}, [slots.default()]),
				divider
			]);
		});
		return {};
	}
});
//#endregion
export { VDivider as t };
