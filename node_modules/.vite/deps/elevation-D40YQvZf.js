import { $n as toRef, zn as isRef } from "./vue.runtime.esm-bundler-CZzmpwlB.js";
import { St as isNullOrUndefined, ut as propsFactory } from "./defineComponent-BW8G7rbF.js";
//#region node_modules/vuetify/lib/composables/elevation.js
var makeElevationProps = propsFactory({
	elevation: {
		type: [Number, String],
		validator: (value) => parseInt(value) >= 0
	},
	hoverElevation: {
		type: [Number, String],
		validator: (value) => parseInt(value) >= 0
	}
}, "elevation");
function useElevation(props) {
	return { elevationClasses: toRef(() => {
		const elevation = isRef(props) ? props.value : props.elevation;
		const hoverElevation = isRef(props) ? null : props.hoverElevation;
		return [...isNullOrUndefined(elevation) ? [] : [`elevation-${parseInt(elevation)}`], ...isNullOrUndefined(hoverElevation) ? [] : [`hover-elevation-${parseInt(hoverElevation)}`]];
	}) };
}
//#endregion
export { useElevation as n, makeElevationProps as t };
