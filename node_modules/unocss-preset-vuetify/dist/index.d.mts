import { Preset, Variant } from 'unocss';

type ElevationOptions = 'md2' | 'md3' | Record<string, Record<string, string>>;

type TypographyOptions = 'md2' | 'md3' | Record<string, Record<string, string | number>>;

declare const defaultBreakpoints: {
    sm: string;
    md: string;
    lg: string;
    xl: string;
    xxl: string;
};
declare const defaultSpacer = 4;
declare const defaultSpacerSteps = 16;
declare const fontWeights: {
    thin: number;
    light: number;
    regular: number;
    medium: number;
    semibold: number;
    bold: number;
    black: number;
};
declare const borderRadii: {
    '0': string;
    sm: string;
    '': string;
    lg: string;
    xl: string;
    pill: string;
    circle: string;
    shaped: string;
};
type TypographyPreset = Record<string, Record<string, string | number>>;
declare const typographyPresets: {
    md2: TypographyPreset;
    md3: TypographyPreset;
};
type ElevationPreset = Record<string, Record<string, string>>;
declare const elevationPresets: {
    md2: ElevationPreset;
    md3: ElevationPreset;
};
type RuleGroup = 'borders' | 'grid' | 'colors' | 'cursor' | 'display' | 'elevation' | 'flex' | 'float' | 'opacity' | 'overflow' | 'pointerEvents' | 'position' | 'sizing' | 'spacing' | 'typography';
interface PresetVuetifyOptions {
    prefix?: string;
    spacer?: number;
    spacerSteps?: number;
    typography?: TypographyOptions;
    elevation?: ElevationOptions;
    breakpoints?: Record<string, string>;
    important?: boolean;
    font?: Record<string, string>;
    themes?: string[];
    /** Rule groups to exclude from the preset */
    exclude?: RuleGroup[];
}

declare function presetVuetify(options?: PresetVuetifyOptions): Preset;

declare function createThemeVariants(themes: string[], options?: {
    order: number;
}): Variant[];

export { borderRadii, createThemeVariants, defaultBreakpoints, defaultSpacer, defaultSpacerSteps, elevationPresets, fontWeights, presetVuetify, typographyPresets };
export type { ElevationOptions, PresetVuetifyOptions, RuleGroup, TypographyOptions };
